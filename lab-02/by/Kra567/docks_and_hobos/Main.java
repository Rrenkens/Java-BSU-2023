package by.Kra567.docks_and_hobos;

import by.Kra567.docks_and_hobos.tunnels.Tunnel;

import java.util.ArrayList;

public class Main {
    public static void main(String[] args) throws InterruptedException {
        /*
        Tunnel<Integer, RandomStorage<Integer>> tunnel = new Tunnel<>(new RandomStorage<>(),5);
        ArrayList<Thread> threads = new ArrayList<>(1000);
        for (int i = 0;i < 1000;i++){
            int k = i;
            Thread thread = new Thread(() -> {
                tunnel.add(k);});
            thread.start();
            threads.add(thread);
        }
        for (Thread t : threads){
            t.join();
        }
        System.out.println(tunnel.getStorage().getData());

         */
    }
}
